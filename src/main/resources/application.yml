spring:
  application:
    name: estribadobank
  datasource:
    url: jdbc:postgresql://localhost:5432/estribadobankdb
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true